group 'codesparkle'
version '1.0'

apply plugin: 'application'

mainClassName = 'com.codesparkle.poker.client.Main'

repositories {
    mavenCentral()
}

dependencies {
    compile files("lib/JGameGrid.jar")
    compile project(':shared')
}
